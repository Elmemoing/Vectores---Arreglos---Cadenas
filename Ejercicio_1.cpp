
#include <stdio.h>
#include <stlib.h>

#define MAX 100

int main()
{
  int x, numero[MAX];
  
  for (x=0; x<=numero[MAX]; x++)
      {
        numero[MAX]=x;
      }
  for (x=0; x<=numero[MAX]; x++)
      {
        printf("%d",numero[MAX]);
      }
  return 0;
}  
